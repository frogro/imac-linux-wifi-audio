# DKMS configuration for cs8409-dkms
PACKAGE_NAME="cs8409-dkms"
PACKAGE_VERSION="2025.09.04-1"
AUTOINSTALL="yes"

# Upstream baut aus dem Repo-Root
MAKE[0]="make"
CLEAN="make clean"

# Gebautes Artefakt (Dateiname ohne .ko, mit Bindestrichen)
BUILT_MODULE_NAME[0]="snd-hda-codec-cs8409"

# Ziel-Modulname (Unterstriche) & Installationsort (mit hoher Priorität)
DEST_MODULE_NAME[0]="snd_hda_codec_cs8409"
DEST_MODULE_LOCATION[0]="/updates/dkms"

# Dort legt der Upstream das .ko meist ab
BUILT_MODULE_LOCATION[0]="build/hda"
